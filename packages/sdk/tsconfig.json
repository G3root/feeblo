{
  "extends": "@feeblo/config/tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "types": [],
    // This config is for typechecking and the editor. `tsconfig.build.json`
    // owns `rootDir`/`outDir`/`declaration` for the real emit.
    "noEmit": true,
    "strictNullChecks": true
  },
  "include": ["src/**/*"]
}

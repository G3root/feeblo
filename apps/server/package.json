{
  "name": "server",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "dotenvx run -f ../../.env -- bun run --watch src/index.ts",
    "start": "dotenvx run -f ../../.env -- bun run src/index.ts"
  },
  "dependencies": {
    "@feeblo/auth": "workspace:*",
    "@feeblo/db": "workspace:*",
    "@feeblo/domain": "workspace:*",
    "@feeblo/utils": "workspace:*",
    "effect": "catalog:",
    "@effect/platform-bun": "catalog:",
    "@effect/platform": "catalog:"
  },
  "devDependencies": {
    "@feeblo/config": "workspace:*"
  }
}

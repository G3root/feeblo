{
  "name": "@feeblo/e2e",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:migrate": "tsx scripts/migrate-pglite.ts",
    "dev:server:e2e": "pnpm run db:migrate && pnpm --dir ../apps/server exec tsx src/index.ts",
    "dev:web:e2e": "pnpm --dir ../packages/sdk build && pnpm --dir ../apps/web exec astro build && pnpm --dir ../apps/web exec astro preview --host 127.0.0.1 --port 3101",
    "test": "playwright test",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
